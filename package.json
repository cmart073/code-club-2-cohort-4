{
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"deploy": "wrangler publish index.js",
		"dev": "wrangler dev index.js --local"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^3.16.0",
		"wrangler": "^2.0.0"
	}
}
